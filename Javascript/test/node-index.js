var qunit = require('qunit');

/*
qunit.run({
    code: {

		// Load dependencies
		deps: ['./worker/metaTags.js', './worker/lib/encoding-indexes.js', './worker/lib/encoding.js'],

		// Include the source code
		path: './worker/encoderWorker.js',

		// What global var should it introduce for your tests?
		namespace: 'Recorder'

    },
    tests: [

		// Include the test suite(s)
		'./test/encoderWorker.test.js'

    ]
});
*/